# Render.com Environment Variables Template
# Copy these to Render Dashboard → Environment → Add Environment Variable

# ===== Required Variables =====

# Node Environment
NODE_ENV=production

# Server Port (Render provides this automatically, but you can override)
PORT=5001

# MongoDB Connection (from MongoDB Atlas)
MONGODB_URI=mongodb+srv://aitools_user:YOUR_PASSWORD_HERE@cluster0.xxxxx.mongodb.net/aitools?retryWrites=true&w=majority

# JWT Secret (generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET=your_64_character_secret_here_generate_with_crypto

# JWT Token Expiration
JWT_EXPIRE=7d

# Frontend URL (GitHub Pages)
FRONTEND_URL=https://kingnets.github.io/Tehism-istus

# ===== Email Configuration (Optional) =====

# Gmail SMTP (if you want email notifications)
EMAIL_USER=tehismoistus@gmail.com
EMAIL_PASSWORD=your_gmail_app_password_here

# ===== Instructions =====

# 1. MongoDB Atlas Setup:
#    - Create free cluster at https://www.mongodb.com/cloud/atlas/register
#    - Create database user with password
#    - Get connection string and replace YOUR_PASSWORD_HERE

# 2. JWT Secret Generation:
#    Run in terminal: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
#    Copy the output and paste above

# 3. Gmail App Password (if using email):
#    - Enable 2FA on your Google account
#    - Go to https://myaccount.google.com/apppasswords
#    - Generate new app password
#    - Copy the 16-character code (remove spaces)

# 4. Add these to Render:
#    Dashboard → Your Service → Environment → Add Environment Variable
#    Copy each KEY=VALUE pair (one per variable)
